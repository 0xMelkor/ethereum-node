#apiVersion: v1
#kind: Service
#metadata:
#  name: geth-np-tcp
#spec:
#  type: NodePort
#  ports:
#  - name: geth-p2p-tcp-np 
#    protocol: TCP
#    port: 30603
#    targetPort: 30603
#    nodePort: 30603
#  selector:
#    app: crystal-mev
#    comp: geth-node-pod
# ---
apiVersion: v1
kind: Service
metadata:
  name: geth-np-udp
spec:
  type: NodePort
  ports:
  - name: geth-p2p-udp-np 
    protocol: UDP
    port: 30603
    targetPort: 30603
    nodePort: 30603
  selector:
    app: crystal-mev
    comp: geth-node-pod