apiVersion: v1
kind: Service
metadata:
  name: geth-execution-service
  labels:
    app: crystal-mev
spec:
  type: ClusterIP
  selector:
    app: crystal-mev
    comp: geth-node-pod
  ports:
    - name: execution-endpoint
      port: 8551
      protocol: TCP
      targetPort: 8551