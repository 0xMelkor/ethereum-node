apiVersion: v1
kind: Service
metadata:
  name: geth-p2p-tcp-service
  labels:
    app: crystal-mev
spec:
  type: NodePort
  selector:
    app: crystal-mev
    comp: geth-node-pod
  ports:
    - name: p2p-tcp-endpoint
      port: 30303
      protocol: TCP
      targetPort: 30303
