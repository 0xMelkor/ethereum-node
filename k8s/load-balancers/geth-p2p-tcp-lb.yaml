apiVersion: v1
kind: Service
metadata:
  name: geth-p2p-tcp-lb
  labels:
    app: crystal-mev
spec:
  type: LoadBalancer
  selector:
    app: crystal-mev
    comp: geth-node-pod
  ports:
    - name: p2p-tcp-lb
      port: 30303
      protocol: TCP
      targetPort: p2p-port-tcp
