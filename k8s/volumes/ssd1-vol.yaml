apiVersion: v1
kind: PersistentVolume
metadata:
  name: ssd1-local-pv
  labels:
    app: crystal-mev
spec:
  capacity:
    storage: 375Gi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: "premium-rwo"
  local:
    path: /mnt/disks/ssd1
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - gke-mev-cluster-default-pool-dd10e5c4-vjzg
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lighthouse-volume
  labels:
    app: crystal-mev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    limits:
      storage: 300Gi 
    requests: 
      storage: 300Gi 
  selector:
    matchLabels:
      app: crystal-mev
  storageClassName: "premium-rwo"
